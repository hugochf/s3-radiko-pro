/*
 * Boot splash asset. (Station logos moved to the `logos` flash partition in
 * Phase 30 — see the stations component.)
 */
#pragma once

#include "lvgl.h"

// Boot splash (radiko logo pre-composited on C_BG, 300px wide).
extern const lv_image_dsc_t SPLASH_IMG;
