/*
 * Copy to wifi_secrets.h (gitignored) and fill in your test network.
 * Leave WIFI_PASSWORD empty ("") for an open network.
 *
 *   cp wifi_secrets.h.example wifi_secrets.h
 */
#pragma once

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"
